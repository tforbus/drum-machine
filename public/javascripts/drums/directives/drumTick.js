angular.module('drums.directives')

/**
 * Toggles the color and state of the drum tick.
 */
.directive('drumTick', function () {
  return {
    restrict: 'A',
    link: function (scope, elem, attrs) {
    }
  };
});
